# 2026-07-24 工作日志

## 阅读器右侧文字遮挡修复
- 问题：分页阅读时每页右侧的文字被 `overflow:hidden` 裁切，显示不全
- 原因：列宽计算 `pw = clientWidth - 6` 的右边距不足，亚像素渲染导致右侧文字被裁
- 修复：将 `initPagination()` 和 `goToPage()` 中的 `-6` 改为 `-12`，增大右边距
- 修改文件：`reader/百年孤独-阅读器.html` 第 1199、1245 行

## 手机版阅读区左右留白不均修复
- 问题：手机版右侧留白比左侧多 12px（列宽收窄全部在右侧），用户感知为左侧留白太多、右侧太少
- 修复：给 `.reader-pages` 增加 `padding: 0 6px`，将 12px 空余均分到左右两侧
  - `clientWidth` 包含 padding，`pw = clientWidth - 12` 后列宽刚好等于内容区宽
  - 每页左右各有 6px 等距留白，且不影响右侧防裁切效果
- 修改文件：`reader/百年孤独-阅读器.html` 第 260 行

## 书架页（藏书阁）
- 新增 `reader/bookshelf.html`：暖纸艺术风书架，3D 立体书（书脊色用 canvas 从封面取样）+ 木质搁板 + 描金进度线，明暗双主题（key `rc-shelf-dark`）
- 书目注册表在文件内 `BOOKS` 数组，新增书加一行即可
- 改造 `reader/app.js`：支持 `index.html?pack=xxx.bookpack` 加载指定书包；进度 key 按书包区分（百年孤独保持 `rc-buddy-cien-anos-v2` 兼容，其他书为 `rc-buddy-<书名>`）
- `reader/index.html` 顶栏新增 📚 返回书架入口
- 书架进度读取：threshold.chapter / total_chapters

## 书架分页
- 书架改为多排搁板 + 分页：桌面每页 15 本（5 本/排 × 3 排），手机（≤640px）每页 6 本（2 本/排 × 3 排），规则在 `RULE_DESKTOP/RULE_MOBILE`
- 翻页器只有多页时才显示；横竖屏切换自动按新规则重排
- 书尺寸缩小为 CSS 变量 `--bw/--bh`（桌面 140×210，手机 126×189）
- 默认书目：百年孤独.bookpack + 生命中不能承受之轻.bookpack（BOOKS 数组）

## 坑：静态预览看不到书包数据
- WorkBuddy 的 static-html 预览只复制单个 HTML，没有 .bookpack/vendor → 封面占位、阅读器 boot 报错
- 必须通过 `http://localhost:8765/bookshelf.html` 访问（reader/ 下 `python3 -m http.server 8765`）
- 已加兜底：书架所有书加载失败时显示提示横幅；app.js boot 错误文案也改为同样指引

## 书架：上传 + 14 本演示
- 删除页眉副标题「BIBLIOTHÈQUE · …」
- 新增 `reader/upload-store.js`：IndexedDB（库 rc-shelf-store / 表 packs）存上传的 .bookpack，bookshelf 与 app.js 共用
- 书架右上角「＋上架书籍」按钮 + 整页拖拽上架（多文件）；上传书悬停纱罩右上角有 × 下架钮（连带清进度）
- 上传书阅读链路：`index.html?pack=upload:<文件名>` → app.js 从 PackStore 读取；进度 key `rc-buddy-upload:<名>`
- 演示：两本默认书 ×7 重复（DEMO_REPEAT=7，SHELF_BOOKS）共 14 本——桌面 1 页（5+5+4），手机 3 页出翻页器

## 分页留白过大修复
- 问题：`break-inside: avoid-column` 禁止段落跨列分断，长段落在页尾放不下时整段推至下一页，产生大片空白
- 修复：删除 `.chapter-content p` 的 `break-inside: avoid-column`，允许段落自然跨页分断
- 修改文件：`reader/百年孤独-阅读器.html` 第 274 行

## 翻页对齐修复（核心问题）
- 问题：翻页后内容错位跨页。`initPagination()` 用 `pw` 设列宽，`goToPage()` 每次重算 `pw = clientWidth - 12`。`clientWidth` 波动导致 `translateX` 与列边界不匹配，翻页越多误差累积越大
- 修复：
  1. 新���全局变量 `SPAN = pw + PAGE_GAP`，在 `initPagination()` 时一次性计算
  2. `goToPage()` 翻页时直接用 `SPAN`，不再重复读 `clientWidth`
  3. `computePageMapping()` 改为用 `SPAN`，居中判定用 `SPAN * 0.5` 替代 `pw / 2`
- 修改位置：第 904、1197-1207、1211-1238、1242-1249 行

## 角色卡片滚动漏透明修复
- 问题：打开角色卡片上下滚动时，内容从圆角处「漏」出，显示透明背景
- 原因：`.char-card` 同时设置 `overflow-y:auto; overflow-x:hidden` 和 `border-radius`，在 Chrome/WebKit 中同轴不同 overflow 值会导致 border-radius 裁剪失效
- 修复：将 `overflow-y:auto; overflow-x:hidden` 合并为 `overflow:auto`
- 修改位置：第 526 行

## 角色卡片滚动穿透主页面修复
- 问题：打开角色卡滚动时，滚到顶部/底部后 scroll 事件穿透到主页面，导致主页面也跟着滚动
- 修复：
  - CSS: `.char-card` 增加 `overscroll-behavior: none`，防止滚动链式传播和弹性回弹
  - JS: `openCharCard()` 打开时锁 body 滚动，`closeCharCard()` 关闭时恢复
- 修改位置：第 527、1471、1484 行

## 问 AI 面板改造
- 改造：
  1. **布局**：`#panel-ask` 改为 flex 列固定高度，`.ask-log` 撑满可滚动，`.ask-input-row`固定在底部。响应式适配 640/480px 断点
  2. **API 接入**：替换 `sendAsk()` 调用阶跃星辰 step-3.7-flash API（OpenAI 兼容），system prompt 含当前进度和已登场角色实现防剧透
  3. **加载动画**：三点跳动加载动画，API 失败时回退到本地 `answerQuery()` 规则引擎
- 新增位置：第 488、501-503、515-519、641、654、915-919、1711-1763 行
  4. **模式切换**：新增「防剧透 / 全书」切换开关，防剧透模式只讨论已读章节，全书模式可讨论任意情节。持久化到 localStorage
  5. **Markdown 渲染**：新增 `renderMarkdown()` 函数支持粗体、斜体、行内代码、代码块、引用、列表、段落。添加对应 CSS 样式
  6. **输入框贴底**：`#panel-ask.active` 改用 `gap` 统一控制间距，去掉独立 margin，`overflow:hidden` 防止溢出，`flex-shrink:0` 固定非滚动区域

## 藏书阁自适应无滚动改造
- 问题：藏书阁页面（bookshelf.html）固定分页（5/排 × 3 排 = 15 本/页），窄屏时内容溢出产生横竖滚动条；固定 max-width: 1100px 也导致宽屏浪费空间
- 改造方案：
  - **CSS**：html/body 设为 `overflow:hidden; height:100vh`，body 用 `display:flex;flex-direction:column` 撑满。去掉 `.shelf-head` 和 `.stage` 的 `max-width:1100px`。stage 加 `overflow:hidden;min-height:0`
  - **CSS 变量化**：新增 `--slots-gap`、`--row-pad-x`、`--row-mb` 取代硬编码值，手机端（≤640px）覆盖这些变量即可自适应
  - **JS 自适应算法**：用 `calcLayout()` 替代固定 `RULE_DESKTOP/RULE_MOBILE`，从 CSS 变量读取书尺寸/间距，根据 `stage.clientWidth/Height` 动态计算 `perRow` 和 `rows`（页 = perRow × rows 本）
  - **窗口 resize**：120ms 防抖重排，横竖屏/窗口缩放时自动调整排布，不离屏就不变页面
- 修改文件：`reader/bookshelf.html`

### 二次调整
- 去掉右侧固定竖句「枕上诗书闲处好 · 门前风景雨来佳」
- 架子/书整体下移：stage 加 `justify-content:center` 垂直居中 + padding-top 从 12→20px（手机 8→12px）
- 进度铭牌紧凑化：margin-top 14→10px, height 38→32px（每行省 ~10px，确保 3 排行得通）
- 行数上限 4 排 → 用户要求改为最多 **3 排**

## 阅读页面桌面端单视口改造
- 问题：桌面端阅读页（index.html）有垂直滚动，阅读区 + 侧栏加起来超过视口高度
- 改造（`reader/styles.css`）：
  - 桌面端（≥961px）：body `overflow:hidden`，`#panel-read` 固定 `height:calc(100vh - topbar-h)` 并 `overflow:hidden`，整页不滚动
  - `.reader-body` 去掉 `align-items:start`（改为 stretch），`height:100%` 撑满面板
  - `.reader-article` 去掉 `max-height:880px` 限制，用 `height:100%` 撑满 grid 单元格
  - `.reader-aside height:100%` 撑满右侧栏；最后一个 `.aside-box`（已出场人物）`flex:1;min-height:0;overflow-y:auto`，内容超出可内部滚动
  - 手机端（≤960px）用 `#panel-read{height:auto;overflow:visible}` 还原
  - `#main` 默认 `padding-bottom:0`，手机端恢复 40px，避免超大屏撑太多行

### ⚠️ 回滚：阅读页面过度工程化修复
- 用户反馈单文件版（`百年孤独-阅读器.html`）布局正确，`http://localhost:8765/index.html` 被改坏了
- 还原操作：`styles.css` 全部回退到与单文件版一致
  - `.reader-body` 恢复 `align-items:start`
  - `.reader-article` 恢复 `height:calc(100vh - var(--topbar-h) - 60px)` + `max-height:880px`
  - `#main` 恢复 `padding:0 20px 40px`
  - 删除整个 `@media(min-width:961px)` 规则块
  - 手机端去掉额外加的 `#panel-read{height:auto;overflow:visible}` 和 `max-height:none`
- 教训：单文件版自带的 `reader-article{height:calc(100vh - 58px - 60px)}` 已天然限制阅读区不超出视口，无需额外加桌面端规则去控制

## app.js 翻页误差修复（与单文件版同步）
- 问题：`goToPage()` 每次翻页重新读 `clientWidth`，微小的像素波动导致 `translateX` 与列边界不匹配，翻页越多误差累积越大
- 修复：引入全局 `SPAN` 变量（= pw + PAGE_GAP），`initPagination()` 时一次性计算，`goToPage()` 和 `computePageMapping()` 都直接用 `SPAN`
- 具体改动（`reader/app.js`）：
  - 新增 `let SPAN=0;` 全局变量
  - `initPagination()`：`SPAN=pw+PAGE_GAP`；`computePageMapping()` 去掉参数
  - `computePageMapping()`：去掉 `pw` 参数，用 `SPAN*0.5` 替代 `pw/2`
  - `goToPage()`：去掉 `const pw=clientWidth-6`，直接用 `translateX(${-idx*SPAN}px)`

### 补漏：app.js 缺失的单文件版修复
- `pw = clientWidth - 6` → `-12`（此前只改了单文件版，app.js 漏了）
- `styles.css` 删除残留的 `break-inside: avoid-column`（单文件版已删）
- `index.html` 加 `?v=2` 强制刷新 CSS/JS 缓存
