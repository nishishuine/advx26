# AI 阅读伴侣 · 产品概念设计（v2）

> **一句话定位**：当你读长篇、读复杂叙事时，替你记住"谁是谁"的 AI 阅读伴侣。
>
> **副标题**：不替你把书读完，只是让你再也不用翻回去查"这人是谁"。
>
> **核心交互**：看到不认识的名字 → 点一下 → 卡片弹出看这人是谁 → 继续读

---

## 一、核心理念

### 你遇到的问题

- 读长篇小说/网文/严肃文学时，人物太多记不住
- 看到中间忘了前面发生的情节
- 角色名字相似，经常搞混
- 需要反复翻回去查"这个人之前做了什么"
- 追更/连载时，间隔太久忘了设定
- **特别是《百年孤独》这种书——同名的人有十几个，读到一半彻底崩溃**

### 解决思路

不是让 AI 替你读书，而是让 AI 在你读书时**站在旁边辅助你**——像有一个读完了整本书的朋友，随时回答你的小问题，而且**绝不会剧透**。

### 产品形态

- **MVP 形态**：网页版阅读器（单页应用），顶部/侧边栏用标签页切换各功能模块
- **轻量模式**：浏览器插件 / 阅读器插件（挂载到微信读书网页版、起点、Kindle 等）
- **重度模式**：独立阅读 App（内置 AI 功能），主打 epub 导入阅读

### 页面结构（MVP 网页版）

```
┌──────────────────────────────────────────┐
│  🏠 阅读  │  📊 关系图  │  👤 角色库  │  📖 时间线  │  🤖 追问  │
├──────────────────────────────────────────┤
│                                          │
│          当前激活的功能面板                │
│   （默认打开阅读器，可随时切换）            │
│                                          │
└──────────────────────────────────────────┘
```

所有功能在同一个页面里用标签页切换，无需跳转页面。

### 核心交互闭环

```
读到这里 → "这人是谁来着？"
    │
    ▼
点一下角色名字
    │
    ▼
弹出角色卡片
    ├── 定妆头像（文生图生成）
    ├── 一句话简介（AI 从已读内容提取）
    ├── 角色关系简图（已读范围内）
    └── 点击「问 AI」进入追问模式
    │
    ▼
"哦对，想起来了" → 关掉卡片 → 继续读
```

---

## 二、功能全景

### 1. 角色关系图（核心功能）

关系图有两种形态，服务不同场景：

**① 全局全景图（预处理生成）**
| 子功能 | 说明 |
|--------|------|
| **全书图谱** | 预处理时一次性生成整本书的完整角色关系图谱 |
| **节点=角色头像** | 每个节点显示该角色的定妆头像 + 名字，边标识关系类型 |
| **可交互浏览** | 缩放、拖拽、点击节点查看角色详情 |
| **截图导出** | 一键导出全景关系图，用于读书笔记分享 |

> 这图在预处理时就做好了，阅读过程中**不依赖 API、秒开**。适合"让我看看这些人到底什么关系"的场景。

**② 动态子图（阅读时按需生成）**
| 子功能 | 说明 |
|--------|------|
| **已读范围** | 只展示用户已读章节中出现的人物和关系 |
| **防剧透** | 未出场的角色、未发生的关系不显示 |
| **随进度扩展** | 读到第 X 章，图就扩展到第 X 章 |
| **章节锚点** | 点击节点可看到"这个角色在第几章做了什么" |

> 这图是从预处理好的全量图谱中**按阅读进度截取子图**，不需要重新调 LLM。适合"我看看当前这些人的关系"的场景。

### 2. 角色标识高亮（《百年孤独》杀手锏）

| 子功能 | 说明 |
|--------|------|
| **角色专属颜色** | 每个角色分配一种专属颜色，在全书中统一高亮标注 |
| **同名区分** | 同名角色用 **颜色 + 小角标编号** 区分（何塞·A¹、何塞·A²、何塞·A³...） |
| **图例面板** | 侧边栏展示"颜色 → 角色名"对照表，随时可查 |
| **悬停预览** | 鼠标悬停在高亮名字上，弹出迷你卡片（角色简介 + 关系快照） |

> **标注机制（关键）**：角色名高亮**不用运行时正则匹配**，而是在预处理阶段由 LLM 逐章通读后，直接把原文中每一处角色提及**内联标注**为 `[[c:角色ID]]原文名字[[/]]` 输出，前端只解析标记并着色。
> 好处：① 同名不同人由 LLM 结合全书语境判定归属（正则做不到）；② 别名/绰号/代称也能精确归位；③ 避免"何塞"误伤"何塞·阿尔卡蒂奥"的内部子串。
> **两个必须的保护措施**：① **逐章处理**，绝不让 LLM 一次性重吐整本书（会超输出长度被截断、且可能改字漏字）；② **保真校验**——标注结果剥离标记后必须与原文逐字相等，不等则回退到"按别名映射表做子串插入"。

### 3. 原文名对照

| 子功能 | 说明 |
|--------|------|
| **译名+原文** | 译作角色名后显示原文名（何塞·阿尔卡蒂奥·布恩迪亚（José Arcadio Buendía）），方便跨语言对照理解 |
| **显示密度** | 每章首次出现时显示原文，后续显示译名和原文；可切换"始终/仅首次/不显示" |
| **仅译作生效** | 中文原创书不显示（原文名为空） |

### 4. AI 追问（核心功能 + 严格防剧透）

| 子功能 | 说明 |
|--------|------|
| **即时问答** | 选中任意段落/角色名，问"这人是谁""之前发生过什么" |
| **上下文锚定** | AI **只接收已读章节的内容**，未读内容完全不传入 LLM |
| **关系问答** | 问到人物关系时，**动态生成基于已读范围的临时关系子图**，不展示未读部分的关系 |
| **对比查询** | "A 和 B 什么关系""A 在第 3 章说了什么" |
| **深度追问** | "这个人物的动机是什么""为什么这里他会这么做" |

> **防剧透原则**：LLM 收到的上下文严格截止到当前阅读进度。技术上通过进度指针控制传入文本的截断点，确保模型"不知道后面发生了什么"。

### 4. 听书功能（TTS）

| 子功能 | 说明 |
|--------|------|
| **AI 语音朗读** | 以自然的语音读当前章节 |
| **实时高亮** | 读到哪一句，文字高亮到哪一句（听读同步） |
| **变速播放** | 0.5x → 4x 平滑调节，步进 0.25x |
| **角色声线区分** | 不同角色用不同声线（远期高级功能） |
| **背景播放** | 切到后台或锁屏仍可继续播放 |

### 5. 角色画像 · 文生图

| 子功能 | 说明 |
|--------|------|
| **角色定妆照** | 每个角色生成**一张**专属肖像画（头像/半身），用于关系图节点头像 + 角色卡片展示 |
| **风格统一** | 同一本书的所有角色保持画风一致（固定画风 prompt + seed） |
| **按需重绘** | 如果用户对生成的肖像不满意，可以重新生成 |

### 6. 背景百科

| 子功能 | 说明 |
|--------|------|
| **世界观百科** | 自动提取书中的设定、地理位置、组织势力等 |
| **时间线** | 按时间顺序排列已读范围内的重大事件 |
| **术语表** | 生僻词、专有名词、世界观术语的自动解释 |

---

## 三、关键设计决策

### 预导入机制（"一次处理，终身使用"）

**关键约束：预处理必须全部完成，用户才能开始阅读。**

原因：LLM 需要通读全书才能准确区分每个人物，特别是：
- 同一个人可能有多个名字/绰号/代称（如"何塞·阿尔卡蒂奥·布恩迪亚"也会被称为"老布恩迪亚"、"族长"、"何塞"）
- 同名不同人需要全书语境才能分辨（如《百年孤独》中祖父和孙子都叫何塞·阿尔卡蒂奥）
- 角色高亮需要在原文中标注所有提及该角色的位置，包括别名

所以预处理不可分段进行，必须等 LLM 完成全书分析后，才能给原文做标注。

```
用户导入 epub 文件
    │
    ▼
[预处理阶段] ← 必须全部完成，用户需等待
    │           （进度条 + 趣味提示，如"正在认识 100+ 个角色..."）
    │
    ├── 解压 epub → 提取纯文本 + 分章节
    ├── LLM 全文通读 → 提取所有角色（含别名/绰号/代称）
    ├── LLM 跨章合并 → 同名角色去重、同人异名合并
    ├── LLM 生成角色标注映射表 → {角色ID: [全名, 别名1, 别名2...]}
    ├── LLM 逐章内联标注原文（[[c:角色ID]]…[[/]]）→ 保真校验（剥标记后须等于原文）
    ├── LLM 提取角色视觉描述（外貌、服装、气质特征）
    ├── LLM 提取角色原文名（original_name，如 José Arcadio Buendía）
    ├── 批量文生图 → 每个主要角色生成一张定妆头像
    └── 构建完整知识图谱 + 头像库 → 存入本地缓存（IndexedDB）
    │
    ▼
[阅读阶段] ← 加载缓存，无需重复调用 API
    │
    ├── 角色高亮标识 ▲ 基于预处理标注映射表，渲染时直接着色
    ├── 关系图展示 ▲ 从缓存图谱中读取，节点显示角色头像
    ├── 角色卡片 ▲ 点名字即弹出（头像 + 简介 + 关系简图）
    │
    └── [增量更新] 读到新章节时，只处理新增内容并合并到缓存
```

**为什么这么设计：**
- ✅ 减少反复调用 LLM 的 token 消耗
- ✅ 提高响应速度（毫秒级加载，而不是每次都等 AI）
- ✅ 降低 API 调用失败的概率
- ✅ 支持离线阅读
- ✅ 文生图一次生成，后续直接展示

### 防剧透机制（分层 + 可主动解锁）

**设计原则**：默认不让用户"轻易"被剧透，但不死守。用户主动想看某角色完整介绍时，给出剧透提示 + 二次确认后允许跳转查看。

**默认（防剧透）视图**——阅读器、角色卡片、关系图、AI 追问全部按当前进度过滤：

```text
当前阅读进度: 第 5 章（共 20 章）
· 角色卡片默认简介 = identity + 由"已读章节 key_events"拼装（天然不含后文）
· AI 追问传入上下文 = 第 1~5 章内容 + 第 1~5 章范围内的图谱数据
· 关系图 = 只渲染"第 1~5 章中已出现的角色及关系"（based_on_chapters ≤ 5）
```

**主动解锁（含剧透）视图**——角色卡片提供「查看完整介绍（含剧透）」入口：

```text
点击「查看完整介绍」
│
▼
⚠️ 提示：以下内容可能包含尚未读到的情节，确定查看？
│
▼（用户确认）
展示 full_description（全书视角的完整人物弧光 / 命运）
```

**重要说明**：预处理阶段 LLM 已通读全书，所以"防剧透"是展示层过滤，而非"模型不知道后文"。为此角色数据拆成两份：
- `identity`：中性身份一句话（如"布恩迪亚族长、乌尔苏拉的丈夫"）——安全，随时可展示
- `full_description`：含命运/结局的完整描述——剧透，仅用户确认后展示

卡片默认简介优先用"已读 key_events"动态拼装，保证不含后文。

---

## 四、用户流程

```
导入 epub 文件
    │
    ▼
┌─────────────────────────────┐
│     预处理阶段（必须完成）     │
│                             │
│  [████████░░░░] 68%         │
│  正在认识书中的人物...        │
│  ✓ 找到 127 个角色           │
│  ✓ 识别 43 个别名映射        │
│  ✓ 构建关系图谱中...          │
│  ✓ 生成角色头像...            │
└─────────────────────────────┘
    │
    ▼ （处理完成后自动进入阅读器）
    │
┌──────────────────────────────────────────┐
│ 🏠 阅读  │ 📊 关系图  │ 👤 角色库  │ ...  │
├──────────────────────────────────────────┤
│                                          │
│  [正文区域 - 角色名以彩色高亮显示]         │
│                                          │
│  点击角色名 → 弹出角色卡片                 │
│  ┌──────────────────────┐               │
│  │ [头像] 何塞·阿尔卡     │               │
│  │ 布恩迪亚家族的族长...  │               │
│  │ 与乌尔苏拉是夫妻       │               │
│  │ [问 AI] [关闭]        │               │
│  └──────────────────────┘               │
│                                          │
└──────────────────────────────────────────┘
```

---

## 五、MVP 范围（48 小时黑客松可交付版）

### 核心体验闭环（优先级排序）

| 优先级 | 模块 | MVP 内容 | 技术方案 |
|:------:|------|---------|---------|
| **P0** | **epub 解析** | 支持上传 .epub 文件，解压并提取纯文本 | epub.js |
| **P0** | **角色提取** | 预处理全书时，LLM 提取角色名和关系 | LLM API + 批处理 |
| **P0** | **角色高亮** | 在正文中用颜色标记角色名（含同名区分） | LLM 预处理内联标注 + CSS 着色 |
| **P0** | **关系图** | 基于预处理结果的力导向图 | vis-network / D3.js |
| **P0** | **AI 追问** | 选中文本后弹出浮窗，LLM 回答 | LLM API + 防剧透 prompt |
| **P1** | **听书 + 高亮** | TTS 朗读 + 句子级实时高亮 | Web Speech API |
| **P1** | **变速播放** | 0.5x-4x 步进调节 | Web Speech API rate 属性 |
| **P1** | **文生图** | 每个主要角色生成一张定妆头像（放在关系图节点+角色卡片里） | 生图 API，预处理阶段批量生成 |
| **P2** | **图例面板** | 颜色→角色名对应表 | 纯前端渲染 |

### 阶段二（黑客松后）

- 角色声线区分
- 多本书管理（书架）
- 长篇连载自动分割
- 阅读进度云端同步
- 关系图分享/导出
- 阅读数据市场（用户之间分享某本书的关系图数据）

---

## 六、技术架构

### 数据流

```
用户导入 epub
    │
    ▼
epub.js 解压 + 分章节
    │
    ▼
LLM 批处理引擎
    │
    ├── 逐章提取角色 + 关系 + 关键事件
    ├── 跨章合并（同名角色去重、关系累加）
    └── 生成完整知识图谱 JSON
    │
    ▼
本地缓存（IndexedDB）
    │
    ▼
阅读时加载缓存
    │
    ├── 正文渲染：解析预处理内联标注 [[c:ID]] → 按角色ID着色
    ├── 关系图渲染：读取图谱 → D3.js 力导向图
    ├── AI 追问：截取已读章节文本 + 用户问题 → LLM → 回答
    └── 听书：Web Speech API → 句子分割 → 实时高亮同步
```

### 推荐技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| **前端** | HTML + Tailwind CSS + vanilla JS | 轻量、快、黑客松友好 |
| **epub 解析** | epub.js | 成熟开源库 |
| **关系图** | vis-network | 比 D3.js 上手快，适合 MVP |
| **LLM** | DeepSeek V4 / GPT-4o-mini | 性价比高 |
| **文生图** | 阶跃星辰 Step API / DALL-E 3 | 有免费额度 |
| **TTS** | Web Speech API | 零成本，支持 rate 控制 |
| **存储** | IndexedDB（via idb-keyval） | 存缓存图谱 + 生图结果 |

---

## 七、Prompt 设计（关键）

### 批量角色提取 Prompt

```
你是一个阅读辅助 AI。以下是用户即将阅读的书籍全文（已分章节）。

你的任务是通读全书，提取所有角色信息。特别注意：
- 同一个人可能在不同章节有不同的称呼（全名、昵称、绰号、代称）
- 不同的人可能共享同一个名字（如《百年孤独》中多人同名）
- 需要全书语境才能判断"这些名字是不是同一个人"

请按照以下格式输出 JSON：

{
  "characters": [
    {
      "id": "唯一标识符（如 character_1）",
      "name": "角色的标准/最常用名称",
      "original_name": "角色在原著语言中的名字（如西班牙语 José Arcadio Buendía），中文原创书留空",
      "aliases": ["别名1", "别名2", "昵称", "绰号", "代称"],
      "first_chapter": 首次出场的章节编号,
      "identity": "中性的一句话身份，只说结构性信息（家族/职业/与谁的关系），不含命运结局，安全可随时展示，不超过30字",
      "full_description": "综合全书视角的完整描述（性格、身份、命运/结局概述），含剧透，仅用户主动确认后展示，不超过80字",
      "visual_description": "用于 AI 生图的角色外貌描述：五官特征、服装风格、气质、年龄、典型造型，约50字",
      "relations": [
        {"target_id": 关联角色ID, "type": "敌对/友好/血缘/爱情/其他", "based_on_chapters": [依据章节]}
      ],
      "key_events": [
        {"chapter": 章节, "event": "该角色在本章的关键事件"}
      ]
    }
  ],
  "alias_mapping": {
    "文本中出现的具体名字字符串": "对应的角色ID",
    "老布恩迪亚": "character_1",
    "族长": "character_1",
    "何塞": "character_1"
  }
}

约束：
- alias_mapping 是关键！请遍历全文，把每一个对同一个人的不同称呼都映射到对应的角色ID
- aliases 数组用于收录所有别名/昵称/绰号
- 特别注意同名不同人的情况，用 id 区分，并在 description 里注明
- 每个 relation 都必须标注依据的章节编号
- 不要编造文本中没有的信息
- original_name 填角色在原著语言中的名字；无法确定或本书为中文原创时留空字符串，不要音译回猜
```

### 角色内联标注 Prompt（逐章，用于高亮）

```
以下是《书名》第 {chapter} 章的原文，以及本书已确认的角色-别名映射表：

{alias_mapping}

任务：把原文中每一处指代某个已知角色的名字（含全名/别名/绰号/代称）用标记包裹：
[[c:角色ID]]原文里的原始名字[[/]]

严格规则：
- 只包裹名字本身，不改动/删除/增加任何其他字符
- 同名不同人：结合上下文判断这里指谁，套用对应角色ID
- 输出必须是"加了标记的完整原文"，剥掉标记后与输入原文逐字一致
- 不确定归属的名字，宁可不标，也不要标错
```

前端拿到后先做保真校验（剥离标记 == 原文），通过则解析 `[[c:ID]]…[[/]]` 着色；失败则回退用 alias_mapping 做子串插入。

### AI 追问 Prompt（严格防剧透）

```
你是一个阅读辅助 AI。用户正在读一本书。

【用户当前的阅读进度】：第 {current_chapter} 章 / 共 {total_chapters} 章

【以下是用户已读章节的内容摘要和角色信息】：
{已读章节的角色图谱数据}

【用户的问题】：
{user_question}

请回答用户的问题，严格遵守以下规则：
1. **只基于已读内容回答**（第 1 章到第 {current_chapter} 章）
2. 绝对不要提及任何尚未读到的人物、事件、关系
3. 如果用户问到的内容前文还没出现过，默认回答："这部分内容在后面才会提到，继续阅读就会揭晓"；但若用户明确表示"我不怕剧透/直接告诉我"，先给一句剧透提示，再回答
4. 如果用户问"XX 和 YY 什么关系"，请同时描述他们截止当前进度的关系状态
5. 回答要简洁（不超过150字），口语化，像朋友聊天一样自然
```

---

## 八、我的 Challenge & 机会点

### 🤔 Challenge 1: 0.5x-4x TTS 就够了

已确认。Web Speech API 原生支持 rate 0.5-2 左右，4x 需要用 `AudioContext.playbackRate` 做音频变速，技术上可行。MVP 直接用 Web Speech API 的 rate 属性做到 2x，后续用 AudioContext 扩展到 4x。

### 🤔 Challenge 2: 预处理等待时间怎么处理？

预处理必须全部完成才能开始阅读，用户确实需要等待。

以《百年孤独》（约 10 万字）估算：
- LLM 全文通读 + 角色提取 + 别名合并 → 按章节分批调用，约 10 章 × 5-10 秒 = 50-100 秒
- 文生图（假设 20 个主要角色）→ 20 次 × 10-20 秒 = 200-400 秒

**总等待时间：约 4-8 分钟。**

**怎么让等待不痛苦：**
- 进度条分阶段显示："正在认识角色…" → "正在整理关系…" → "正在绘制头像…"
- 每个阶段显示趣味信息，如"发现第 17 个何塞·阿尔卡蒂奥…"
- 预处理完成后推送通知（或播放提示音）
- 提供示例书让用户导入后先去倒杯咖啡 ☕

### 🤔 Challenge 3: epub 渲染

你说得对，渲染 epub 本身不难 —— epub.js 等成熟库可以直接在网页里渲染完整 epub 内容，包括样式、图片、章节导航等。

**但我们的特殊之处在于：要在 epub 渲染的纯文本层上叠加角色高亮。**

方案：用 epub.js 把 epub 渲染到网页里，然后解析预处理内联标注来着色。或者更简单：epub.js 提取纯文本，我们自己渲染文本并叠加高亮。

**MVP 就用网页版 + 纯文本渲染**，不做完整 epub 排版。后续可以升级到 iframe 渲染原版 epub + 覆盖层标记。

### 🤔 Challenge 4: 同名角色在《百年孤独》里的具体方案

这本书有 17+ 个何塞·阿尔卡蒂奥和奥雷里亚诺。光靠颜色和编号不够。

**方案**：
- **全名缩写 + 编号**：何塞·阿尔卡蒂奥·布恩迪亚 → **何·阿·布 (①)**，何塞·阿尔卡蒂奥（儿子）→ **何·阿 (②)**
- **颜色 + 家族分支标识**：布恩迪亚家族用蓝色系（深蓝、浅蓝、天蓝...），不同分支用不同色调
- **点击可看全名**：高亮名字旁有 ⓘ 图标，点击展开完整角色卡片
- **归属由预处理标注决定**：每处"何塞"到底是 ① 还是 ②，是 LLM 通读全书后内联标注好的角色 ID，不是运行时靠正则或规则猜的

### 💡 机会点 1: 阅读数据市场

"预导入机制"天然支持一种社交功能：

> **用户分享某本书的关系图谱**
> - 你做完《百年孤独》的关系图，可以导出分享
> - 其他读者导入这本书时，可以选择"使用社区的图谱数据"
> - 类似"读书笔记共享"，但这比笔记更结构化

这可能是产品冷启动的**增长飞轮**——每多一个人做一本书的图谱，社区就多一份资产。

### 💡 机会点 2: 角色颜色高亮可以做成"阅读风格主题"

- "伏笔模式"：后期剧情有重要作用的角色用更醒目的颜色
- "关系模式"：颜色按阵营/家族分组（百年孤独里蓝色系=布恩迪亚家族，红色系=外人）
- "自定义模式"：用户可以手动调整某个角色的颜色

### 💡 机会点 3: AI 笔记锚点

用户可以在某个人物/事件上"钉"一个 AI 生成的笔记卡片，类似：
> "奥雷里亚诺上校在这里第一次表现出对权力的渴望——这一点会在第 12 章他成为司令时再次出现"

这个笔记锚点是 AI 根据**未读内容生成的伏笔提示**（可选开关）——有些用户愿意接受轻微暗示，有些不愿意。

### 💡 机会点 4: 对标/差异化总结

| 竞品 | 他们在做什么 | 我们不同在哪 |
|------|-------------|------------|
| 微信读书 | 社交阅读 + 笔记 | 不做深层角色分析 |
| 得到 | 听书 + 解读 | AI 不会实时回答追问 |
| Fade In / 文学工具 | 编剧工具，做角色卡 | 不是为阅读场景设计的 |
| ChatPDF | 解析文档 + 问答 | 没有角色追踪、关系图、听书 |
| **我们** | **阅读过程辅助** | **不替用户读书，帮用户更好地读** |

---

## 九、为黑客松准备的 Pitch 一句话

> "我不会忘记角色名字了——因为 AI 正在替我记着。"

### 30 秒 Pitch

> "你有没有读《百年孤独》读到一半，发现出现了第 5 个何塞·阿尔卡蒂奥，然后彻底放弃的？
>
> 我做了一个 AI 阅读伴侣：导入 epub，它自动提取所有角色，给每个人分配专属颜色在全书中高亮标记。你读到哪，它跟到哪。不认识的人——悬浮看卡片。忘了前情——侧滑看关系图。读到一半想确认——问它，它只说你已经读过的内容，绝不剧透。
>
> 不替你把书读完，只是让你再也不用翻回去查'这人是谁'。"

---

## 十、后续建议

- **黑客松优先顺序**：epub 解析 → 角色提取预处理 → 角色高亮 → 关系图 → AI 追问 → TTS → 文生图
- **赛道建议**：Qoder（04）或智能少年（05），这两个对你的产品理念最友好
- **TTS 用 Web Speech API**——零成本、秒上线，支持 rate 控制
- **文生图放最后**——先做角色提取 + prompt 组装，留好接口，图可以预生成后展示
- **用 Qoder 的话**：让 Agent 分工——一个写 epub 解析、一个写前端阅读器、一个写 LLM 编排、一个写关系图渲染
