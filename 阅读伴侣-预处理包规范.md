# AI 阅读伴侣 · 预处理产物包规范（v1）

> 一句话：阅读器只吃这个"包"，绝不在运行时解析原始 epub。预处理阶段把 epub 的混乱结构**全部归一化**后打进包里。

---

## 0. 设计原则

1. **阅读器零解析原文格式**：epub 里章节和文件不是一一对应（如《百年孤独》一个 html 塞了好几章、靠锚点分界），这些脏活预处理时干完，包里只有干净的第 1~N 章。
2. **一切资源本地化**：正文、角色、关系图、头像、颜色全部打进包，阅读器加载即用，不依赖网络、不再调 LLM。
3. **防剧透靠数据分层**：每条数据都带"依据章节"，阅读器按进度过滤；剧透内容单独字段，UI 主动确认后才展示。
4. **可校验**：正文标注必须能通过"保真校验"（剥掉标记 == 原文）。

---

## 1. 包的总体结构

包本质是一个 zip（建议后缀 `.bookpack`，就是普通 zip，网页里用 JSZip 解压）：

```
百年孤独.bookpack (zip)
├── manifest.json          # 包元信息 + 版本 + 文件清单
├── book.json              # 书籍元数据 + 章节索引（归一化后的 N 章）
├── chapters/              # 逐章正文（已内联标注角色）
│   ├── ch01.json
│   ├── ch02.json
│   └── ... ch20.json
├── characters.json        # 角色库（identity / full_description / 颜色 / 角标 / 关系 / 事件）
├── alias_mapping.json     # 名字字符串 → 角色ID（高亮回退用）
├── graph.json             # 关系图谱（nodes + edges，边带 based_on_chapters）
├── theme.json             # 颜色分配 / 家族分支色系（可选）
├── portraits/             # 角色定妆头像
│   ├── c01.png
│   ├── c02.png
│   └── ...
└── cover.jpg              # 封面（可选）
```

---

## 2. manifest.json —— 包的身份证

```json
{
  "schema_version": "1.0",
  "package_type": "reading-companion-book",
  "book_id": "cien-anos-de-soledad",
  "generated_at": "2026-07-23T10:00:00Z",
  "generator": "preprocess-pipeline@0.1",
  "source": { "filename": "百年孤独原版.epub", "format": "epub" },
  "stats": { "chapters": 20, "characters": 47, "portraits": 47 },
  "integrity": {
    "text_verified": true,
    "verified_at": "2026-07-23T10:00:00Z"
  }
}
```

- `schema_version` 用于阅读器兼容判断，往后升级格式靠它。
- `integrity.text_verified` = false 时阅读器应提示"该包正文标注未校验，可能有偏差"。
- ⚠️ **本文所有 JSON 示例为了可读性可能含 `//` 说明文字；真实产出必须是标准 JSON（无注释、无尾逗号）。**

---

## 3. book.json —— 章节索引（归一化的核心）

```json
{
  "title": "百年孤独",
  "author": "加西亚·马尔克斯 / 范晏 译",
  "language": "zh",
  "source_language": "es",
  "show_original_name": "first",
  "cover": "cover.jpg",
  "total_chapters": 20,
  "chapters": [
    { "index": 1, "id": "ch01", "title": "第一章", "file": "chapters/ch01.json", "char_count": 5231 },
    { "index": 2, "id": "ch02", "title": "第二章", "file": "chapters/ch02.json", "char_count": 4980 }
  ]
}
```

> **归一化要点**：原 epub 里第 1~5 章挤在 `part0003.html` 用锚点分界——预处理必须按 toc.ncx 的锚点切开，产出规整的 `ch01`~`ch20`。阅读器再也见不到 `partXXXX.html` 和 `#calibre_pb_x`。

---

## 4. chapters/chNN.json —— 已标注的正文

每章一个文件，正文按**段落数组**存，每段是一个"内联标注字符串"：

```json
{
  "index": 1,
  "id": "ch01",
  "title": "第一章",
  "paragraphs": [
    "多年以后，面对行刑队，[[c:c02]]奥雷里亚诺·布恩迪亚[[/]]上校将会回想起父亲带他去见识冰块的那个遥远的下午……",
    "自称[[c:c15]]梅尔基亚德斯[[/]]的吉卜赛人当众进行了一场展示……"
  ]
}
```

**标注格式约定（唯一权威定义）**：
- 语法：`[[c:角色ID]]原文里出现的原始名字[[/]]`
- 角色ID 必须能在 `characters.json` 里找到。
- **只包裹名字本身**，不改动、不增删任何其他字符。
- **保真校验**：把所有 `[[c:xx]]` 和 `[[/]]` 删掉后，拼起来的纯文本必须与**清洗后的正文基准**逐字相等（含标点、脚注标记）。注意基准是清洗后的 `chapters` 正文（即本仓库 `cleaned/chapters/*.json`），**不是原始 epub**。预处理产包时就要跑这个校验，写进 `manifest.integrity`。
- 阅读器渲染：解析标记 → `<span class="char" data-cid="c02" style="color:...">奥雷里亚诺·布恩迪亚</span>`。
- **原文名不写进标注**：渲染时按显示规则从 `characters[cid].original_name` 拼出「译名（原文）」，标注格式保持纯净、保真校验不受影响。

> 建议顺手在预处理时**清洗脏空格**（如"布恩迪亚 上校"的多余空格），但清洗后要以清洗后的文本作为"原文基准"来做保真校验，保证标注和展示文本一致。

---

## 5. characters.json —— 角色库

```json
{
  "characters": [
    {
      "id": "c01",
      "name": "何塞·阿尔卡蒂奥·布恩迪亚",
      "original_name": "José Arcadio Buendía",
      "short_name": "何·阿·布",
      "badge": "①",
      "aliases": ["老布恩迪亚", "族长", "何塞"],
      "identity": "布恩迪亚家族族长、乌尔苏拉的丈夫、马孔多创建者",
      "full_description": "……含命运/结局的完整描述，剧透，仅用户确认后展示……",
      "visual_description": "中年男性，络腮胡，粗布衬衫，眼神执拗……（生图用）",
      "first_chapter": 1,
      "color": "#0B3D91",
      "family": "布恩迪亚",
      "portrait": "portraits/c01.png",
      "relations": [
        { "target_id": "c03", "type": "爱情", "label": "夫妻", "based_on_chapters": [1] },
        { "target_id": "c02", "type": "血缘", "label": "父子", "based_on_chapters": [1, 2] }
      ],
      "key_events": [
        { "chapter": 1, "event": "带全家翻山越岭创建马孔多" },
        { "chapter": 3, "event": "痴迷炼金术与各种发明" }
      ]
    }
  ]
}
```

字段职责：

| 字段 | 用途 | 是否剧透 |
|------|------|:------:|
| `identity` | 卡片默认头部、悬停迷你卡 | 安全 |
| `full_description` | 「查看完整介绍」入口，二次确认后展示 | ⚠️剧透 |
| `key_events` | 按 chapter 过滤后拼"截至当前进度的简介" | 安全（过滤后） |
| `short_name`+`badge` | 同名角色区分显示（何·阿·布①） | 安全 |
| `original_name` | 译名后显示原文名（何塞·阿尔卡蒂奥·布恩迪亚（José Arcadio Buendía）） | 安全 |
| `color`+`family` | 高亮着色 / 家族色系分组 | 安全 |
| `portrait` | 关系图节点头像 + 卡片头像 | 安全 |
| `relations[].based_on_chapters` | 关系图按进度过滤 | 用于防剧透 |

> **原文名显示规则**：`original_name` 存在且与译名不同才显示。`book.show_original_name` 取值：
> - `"first"`（推荐默认）：每章**首次**出现该角色时渲染「译名（原文）」，本章后续只显示译名（悬停/卡片仍可见原文）
> - `"always"`：每处都显示（名字多时较吵）；`"never"`：正文不显示，仅卡片/悬停可见
> - 前端按章顺序渲染，自行维护"本章已显示过原文的角色集合"。若 `source_language == language`（中文原创），`original_name` 为空、不显示。
> - 同名不同人往往**原文名也相同**（如两个 José Arcadio），所以原文名不能用于区分，仍靠 `short_name`+`badge`。

---

## 6. alias_mapping.json —— 高亮回退表

主路径是正文里的内联标注；这张表是**保真校验失败时的回退**（用子串插入着色），也可给 AI 追问做名字消歧。

```json
{
  "老布恩迪亚": "c01",
  "族长": "c01",
  "何塞": "c01",
  "奥雷里亚诺上校": "c02"
}
```

> 注意同名歧义：像"何塞"这种可能指多人的名字，回退子串插入只能给一个默认归属，精度不如内联标注——所以内联标注才是主路径。

---

## 7. graph.json —— 关系图谱

可从 characters 派生，但预生成一份能让关系图秒开：

```json
{
  "nodes": [
    { "id": "c01", "label": "何塞·阿尔卡蒂奥·布恩迪亚", "portrait": "portraits/c01.png", "color": "#0B3D91", "family": "布恩迪亚", "first_chapter": 1 }
  ],
  "edges": [
    { "source": "c01", "target": "c03", "type": "爱情", "label": "夫妻", "based_on_chapters": [1] }
  ]
}
```

- **全景图**：直接渲染全部 nodes/edges。
- **动态子图（防剧透）**：只渲染 `node.first_chapter ≤ 当前章` 且 `edge.based_on_chapters` 里存在 `≤ 当前章` 的元素。

---

## 8. theme.json —— 颜色主题（可选）

```json
{
  "families": {
    "布恩迪亚": { "palette": ["#0B3D91", "#1E5FBF", "#4E86E0", "#83AEF0"] },
    "外来者":   { "palette": ["#9B1C1C", "#C0392B", "#E06B5A"] }
  },
  "default_palette": ["#2E7D32", "#00838F", "#6A1B9A", "#EF6C00"],
  "mode": "family"
}
```

> 每个角色的 `color` 已在 characters.json 定好；theme 只是记录分配规则，方便"关系模式/自定义模式"切换。

---

## 9. 阅读器加载流程

```
1. JSZip 解压 .bookpack
2. 读 manifest.json → 校验 schema_version、检查 integrity.text_verified
3. 读 book.json → 渲染书架/目录
4. 按需加载 chapters/chNN.json → 解析 [[c:ID]] 标记 → 用 characters 里的 color 着色
5. 读 characters.json + graph.json + portraits/ → 角色卡片、关系图
6. 全部写入 IndexedDB 缓存，后续离线秒开
```

**防剧透过滤统一入口**：所有视图渲染前，先按"当前阅读章 N"过滤：
- 卡片简介 = `identity` + `key_events(chapter ≤ N)` 拼装
- 关系图 = `first_chapter ≤ N` 的节点 + `based_on_chapters` 含 `≤ N` 的边
- `full_description` 只在用户点「查看完整介绍」并确认后才读

---

## 10. 现场演示打包清单（最小可跑）

预处理《百年孤独》后，确认包里齐了这些就能直接演示：

- [ ] `manifest.json`（`text_verified: true`）
- [ ] `book.json`（20 章，标题/字数齐）
- [ ] `chapters/ch01.json` ~ `ch20.json`（内联标注 + 通过保真校验）
- [ ] `characters.json`（每个角色有 identity / full_description / color / portrait）
- [ ] `alias_mapping.json`
- [ ] `graph.json`
- [ ] `portraits/*.png`（主要角色头像齐）
- [ ] `cover.jpg`
- [ ] `theme.json`（可选）

---

## 11. 版本与扩展

- 往后加字段一律**新增可选字段**，不改已有字段含义，`schema_version` 小版本号 +1。
- 破坏性改动（如改标注语法）才升大版本，阅读器按 `schema_version` 拒绝不兼容的包。
- "阅读数据市场"就是分享这个 `.bookpack`——所以格式稳定很重要。
