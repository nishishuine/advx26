# AI 阅读伴侣 · 预处理产物包规范与实战手册

> **一句话**：阅读器只吃这个"包"，绝不在运行时解析原始格式。预处理阶段把混乱结构**全部归一化**后打进包里。
> 本文档同时定义包的格式规范（"是什么"）和制备方法（"怎么做"），适用于任何虚构类作品（小说、史诗、剧本、群像叙事）的 .bookpack 生成。

---

## 0. 设计原则

1. **阅读器零解析原文格式**：epub 里章节和文件不是一一对应——预处理必须按 toc 锚点切开，产出规整的 `ch01`~`chNN`。阅读器永远见不到 `partXXXX.html` 和 `#calibre_pb_x`。
2. **一切资源本地化**：正文、角色、关系图、头像、颜色全部打进包，阅读器加载即用，不依赖网络、不再调 LLM。
3. **防剧透靠数据分层**：每条数据都带"依据章节"，阅读器按进度过滤；剧透内容单独字段，UI 主动确认后才展示。
4. **可校验**：正文标注必须能通过**保真校验**（剥掉标记 == 原文）。

### 预处理总流程

```
原始文本 → 章节切分清洗 → 角色提取定义 → 正文标注 → 数据丰富 → 打包校验
```

**工具链**：Python 3 标准库（`json`, `re`, `zipfile`, `base64`），无需外部依赖。

---

## 1. 包的总体结构

包本质是一个 zip（建议后缀 `.bookpack`，阅读器用 JSZip 解压）：

```
书名著.bookpack (zip)
├── manifest.json          # 包元信息 + 版本 + 文件清单
├── book.json              # 书籍元数据 + 章节索引（归一化后的 N 章）
├── chapters/              # 逐章正文（已内联标注角色）
│   ├── ch01.json
│   ├── ch02.json
│   └── ...
├── characters.json        # 角色库（identity / full_description / 颜色 / 角标 / 关系 / 事件）
├── alias_mapping.json     # 名字字符串 → 角色ID（高亮回退用）
├── graph.json             # 关系图谱（nodes + edges，边带 based_on_chapters）
├── theme.json             # 颜色分配 / 家族分支色系（可选）
├── portraits/             # 角色定妆头像
│   ├── c01.svg
│   └── ...
└── cover.svg              # 封面（可选）
```

> **实施要点**：产包脚本需要维护 `BUILD_DIR`，所有文件写入此处后统一 zip。不要手动编辑 zip。

---

## 2. manifest.json —— 包的身份证

```json
{
  "schema_version": "1.0",
  "package_type": "reading-companion-book",
  "book_id": "book-id-slug",
  "generated_at": "2026-07-23T10:00:00Z",
  "generator": "preprocess-pipeline@0.1",
  "source": { "filename": "原版.epub", "format": "epub" },
  "stats": { "chapters": 20, "characters": 25, "portraits": 25, "relations": 150, "key_events": 200 },
  "integrity": {
    "text_verified": true,
    "verified_at": "2026-07-23T10:00:00Z"
  }
}
```

- `schema_version` 用于阅读器兼容判断。
- `integrity.text_verified = false` 时阅读器应提示"该包正文标注未校验，可能有偏差"。
- `stats` 字段可供阅读器在加载前显示统计数据。
- ⚠️ **所有 JSON 必须是标准 JSON（无注释、无尾逗号）**。

---

## 3. 预处理第一步：章节归一化

### 原则
阅读器只需要干净的 `ch01.json ~ chNN.json`，永远不要暴露原格式的混乱。

### 操作
1. 从原始文本（epub/txt/md）按目录切分为 N 章
2. 每章存为 `{ "index": N, "id": "chN", "title": "第N章", "paragraphs": [...] }`
3. **清洗脏空格**：如"上 校" → "上校"、"先 生"→"先生"（清洗后的文本作为保真校验的基准）

### book.json —— 章节索引

```json
{
  "title": "书名",
  "author": "作者名 / 译者名",
  "language": "zh",
  "source_language": "en",
  "show_original_name": "first",
  "cover": "cover.svg",
  "total_chapters": 24,
  "chapters": [
    { "index": 1, "id": "ch01", "title": "第一章", "file": "chapters/ch01.json", "char_count": 8500 },
    { "index": 2, "id": "ch02", "title": "第二章", "file": "chapters/ch02.json", "char_count": 9200 }
  ]
}
```

**`show_original_name` 取值说明**：
- `"first"`（推荐默认）：每章**首次**出现该角色时渲染「译名（原文）」，本章后续只显示译名（悬停/卡片仍可见原文）
- `"always"`：每处都显示（名字多时较吵）
- `"never"`：正文不显示，仅卡片/悬停可见

若 `source_language == language`（中文原创），`original_name` 为空、不显示。

### 关键检查项
- [ ] 章节数与目录一致
- [ ] 每章有段落数组（而非大段文本）
- [ ] 无原始格式残留
- [ ] char_count 为剥离标注后的纯文本字数

---

## 4. 角色库构建

### 4.1 角色定义

```json
{
  "characters": [
    {
      "id": "c01",
      "name": "角色全名",
      "original_name": "Original Name",
      "short_name": "角·色·名",
      "badge": "①",
      "aliases": ["角色简称", "外号", "官职"],
      "identity": "身份描述：一句话介绍，不剧透",
      "full_description": "……含命运/结局的完整描述，剧透，仅用户确认后展示……",
      "visual_description": "外貌描述：年龄、衣着、气质……（用于稳定生图）",
      "analysis": "……文学分析/评论，剧透，与 full_description 一同展示……",
      "first_chapter": 1,
      "color": "#0B3D91",
      "family": "阵营或家族名",
      "portrait": "portraits/c01.svg",
      "relations": [
        { "target_id": "c02", "type": "血缘/爱情/友情/仇恨", "label": "具体关系描述", "based_on_chapters": [1, 2] }
      ],
      "key_events": [
        { "chapter": 1, "event": "一件事的描述" }
      ]
    }
  ]
}
```

### 4.2 字段职责表

| 字段 | 用途 | 是否剧透 | 必填 |
|------|------|:--------:|:----:|
| `identity` | 卡片默认头部、悬停迷你卡 | 安全 | ✅ |
| `full_description` | 「查看完整介绍」入口，二次确认后展示 | ⚠️剧透 | ✅ |
| `analysis` | 「查看完整介绍」内，与 full_description 并列展示 | ⚠️剧透 | 推荐 |
| `key_events` | 按 chapter 过滤后拼"截至当前进度的简介" | 安全（过滤后） | ✅ |
| `short_name`+`badge` | 同名角色区分显示（何·阿·布①） | 安全 | ✅ |
| `original_name` | 译名后显示原文名 | 安全 | 有译名差异时 |
| `color`+`family` | 高亮着色 / 家族色系分组 | 安全 | ✅ |
| `portrait` | 关系图节点头像 + 卡片头像 | 安全 | ✅ |
| `relations[].based_on_chapters` | 关系图按进度过滤 | 用于防剧透 | ✅ |
| `visual_description` | 稳定生图用（DALL·E 等） | 安全 | 可选 |

> **同名不同人**，往往**原文名也相同**（如两代同名角色），不能靠 `original_name` 区分，必须用 `short_name`+`badge`。

### 4.3 角色覆盖标准

| 角色类型 | 是否必须 | 说明 |
|---------|:-------:|------|
| 主角（书中反复出现） | ✅ | 完整字段 |
| 重要配角（推动剧情） | ✅ | 至少 identity + key_events |
| 次要配角（出现 1-2 章） | 可选 | 至少 identity + first_chapter |
| 龙套（仅提名字） | 不收录 | alias_mapping 也无需收录 |

### 4.4 命名规范

- `aliases` 中包含**所有**在该书翻译中出现的称呼变体（含含标点变体："·" vs "."）
- 同名歧义时用 `badge` 区分
- `short_name` 要足够短以在卡片中显示

### 4.5 key_events：时间线数据源

时间线渲染完全依赖 `key_events` 数组。要求：
- **每章至少 3-5 条事件**，关键章节 10-20 条
- 每个事件格式：`{ "chapter": N, "event": "一句话描述" }`
- 覆盖该章所有活跃角色
- 事件描述简洁但信息完整

**量化目标**：总事件数 ≈ 角色数 × 每角色平均出场章节 × 1.5

### 4.6 relations：关系图谱数据源

要求：
- A→B 和 B→A 最好都声明（阅读器按进度双向过滤）
- `based_on_chapters` 用于防剧透

**覆盖要求**：
- 直系血缘关系：必须覆盖
- 故事中明确建立的亲密/敌对关系：必须覆盖
- 同住/同期的社交关系：推荐覆盖
- 仅偶遇一次的关系：可省略

**量化目标**：总关系数 ≈ 角色数 × 5~8

### 4.7 analysis：角色分析（可选但推荐）

每段 150-250 字文学评论。内容覆盖：
- 角色在小说中的核心意义
- 其命运所代表的主题
- 与其他角色的核心冲突/联系
- 不回避结局（因为是剧透内容，需二次确认才展示）

---

## 5. 正文标注

### 5.1 章节文件格式

每章一个 JSON，正文按**段落数组**存，每段是一个"内联标注字符串"：

```json
{
  "index": 1,
  "id": "ch01",
  "title": "第一章",
  "paragraphs": [
    "[[c:c01]]主角[[/]]走进房间，看到[[c:c02]]二号角色[[/]]正坐在窗边。",
    "「你来了，」[[c:c02]]二号角色[[/]]说，「[[c:c03]]三号角色[[/]]在等你。」"
  ]
}
```

### 5.2 标注格式（唯一权威定义）

- 语法：`[[c:角色ID]]原文里出现的原始名字[[/]]`
- 角色ID 必须在 `characters.json` 里能找到
- **只包裹名字本身**，不改动、不增删任何其他字符
- **原文名不写进标注**——渲染时从 `characters[cid].original_name` 拼出「译名（原文）」，标注格式保持纯净

### 5.3 两阶段标注法（核心方法）

```
阶段一：主要标注（用全部别名做字符串匹配）
阶段二：回退标注（用 alias_mapping 补漏）
```

**匹配优先级**：
1. 最长匹配优先（"某某角色全名" > "角色简称" > "姓"）
2. 等长时选 first_chapter 最早的角色
3. 禁止嵌套/重叠标注

**最小匹配长度**：建议 ≥2 字符，避免单字误标

### 5.4 歧义消歧

| 歧义类型 | 处理方案 |
|---------|---------|
| 同一名字指向多人（如简称指向多个同名角色） | 选 first_chapter 最早的角色 |
| 短名包含在长名中（如"简称"⊂"角色全名·职位"） | 最长匹配优先自动处理 |
| 同一人在不同章节身份不同 | **不做**上下文感知，标注最匹配的角色 |

### 5.5 保真校验（不可或缺）

```python
def fidelity_check(original_paragraphs, annotated_paragraphs):
    """剥离标注标记后，与原文逐字比较"""
    def strip(text):
        return re.sub(r'\[\[c:\w+\]\]|\[\[\/\]\]', '', text)
    for orig, ann in zip(original_paragraphs, annotated_paragraphs):
        if strip(ann) != orig:
            return False
    return True
```

**要求**：所有章节必须通过保真校验，否则 `manifest.integrity.text_verified = false`，阅读器会提示"标注未校验，可能有偏差"。

> **实施经验**：先清洗脏空格再标注，清洗后的文本作为保真校验的基准。标注完成后剥离标记与基准逐字比较（含标点、脚注标记）。

---

## 6. alias_mapping.json —— 高亮回退表

主路径是正文里的内联标注；这张表是**保真校验失败时的回退**（用子串插入着色），也可给 AI 追问做名字消歧。

```json
{
  "角色简称": "c01",
  "外号": "c01",
  "官职称呼": "c02",
  "全名·姓氏": "c02"
}
```

### 构建规则

```python
def build_alias_mapping(chars):
    # 按 first_chapter 排序，最早出现的角色获得冲突胜利
    entries = [(alias, c.id, c.first_chapter) for c in chars for alias in c.aliases]
    entries.sort(key=lambda x: x[2])
    mapping = {}
    for alias, cid, _ in entries:
        if alias not in mapping:
            mapping[alias] = cid
    return mapping
```

> 同名歧义（如一个简称指向多人），回退子串插入只能给一个默认归属。内联标注才是主路径。

---

## 7. graph.json —— 关系图谱

可从 `characters.relations` 派生：

```json
{
  "nodes": [
    { "id": "c01", "label": "角色全名", "portrait": "portraits/c01.png", "color": "#0B3D91", "family": "阵营A", "first_chapter": 1 }
  ],
  "edges": [
    { "source": "c01", "target": "c02", "type": "爱情", "label": "夫妻", "based_on_chapters": [1] }
  ]
}
```

- **全景图**：直接渲染全部 nodes/edges
- **动态子图（防剧透）**：只渲染 `node.first_chapter ≤ 当前章` 且 `edge.based_on_chapters` 里存在 `≤ 当前章` 的元素

### 自动生成脚本

```python
nodes = [{ "id": c.id, "label": c.name, "color": c.color, ... } for c in chars]
edges = []
seen = set()
for c in chars:
    for r in c.relations:
        key = tuple(sorted([c.id, r.target_id]))
        if key not in seen:
            seen.add(key)
            edges.append({ "source": c.id, "target": r.target_id, ... })
```

---

## 8. theme.json —— 颜色主题（可选）

```json
{
  "families": {
    "阵营A": { "palette": ["#0B3D91", "#1E5FBF", "#4E86E0", "#83AEF0"] },
    "阵营B":   { "palette": ["#9B1C1C", "#C0392B", "#E06B5A"] }
  },
  "default_palette": ["#2E7D32", "#00838F", "#6A1B9A", "#EF6C00"],
  "mode": "family"
}
```

每个角色的 `color` 已在 characters.json 定好；theme 只记录分配规则，方便"关系模式/自定义模式"切换。

---

## 9. 阅读器加载流程

```
1. JSZip 解压 .bookpack
2. 读 manifest.json → 校验 schema_version、检查 integrity.text_verified
3. 读 book.json → 渲染书架/目录
4. 按需加载 chapters/chNN.json → 解析 [[c:ID]] 标记 → 用 characters 里的 color 着色
5. 读 characters.json + graph.json + portraits/ → 角色卡片、关系图
6. 全部写入 IndexedDB 缓存，后续离线秒开
```

**防剧透过滤统一入口**：所有视图渲染前，先按"当前阅读章 N"过滤：
- 卡片简介 = `identity` + `key_events(chapter ≤ N)` 拼装
- 角色列表只显示 `first_chapter ≤ N` 的角色
- 关系图只显示 `first_chapter ≤ N` 的节点 + `based_on_chapters` 含 `≤ N` 的边
- `full_description` 和 `analysis` 只在用户点「查看完整介绍」并确认后才读

---

## 10. 常见坑与排查

### ❌ 时间线空白
**症状**：数据已加载但时间线无内容
**排查**：
1. 检查 `chars.forEach` 中是否有静默异常 → 加 `try-catch`
2. 检查 `c.key_events` 是否为数组 → `if(!Array.isArray(c.key_events)) return`
3. 检查 `e.chapter` 是否为数字 → `if(chNum===undefined||chNum===null) return`
4. 检查阅读器 JS 中 push 的对象属性名与解构是否匹配（push 用 `event:` → 解构用 `{c,event}`）

### ❌ 关系图节点/边不全
**排查**：
1. `relations` 是否在 `characters.json` 中正确声明
2. `graph.json` 是否从最新的 `characters.json` 重新生成
3. 边是否去重（双向关系只存一条）

### ❌ 保真校验失败
**排查**：
1. 标注标记是否嵌套 → 用 `self_avoiding_walk` 防止重叠
2. 标注使用的名字是否与原文完全一致（含标点）
3. 清洗阶段是否有不一致的替换

### ❌ 独立版 HTML 嵌入 base64 没更新
**排查**：
1. 用 `python` 解码检查独立版 HTML 中的 `EMBEDDED_PACK`
2. 解压后验证 `characters.json` 的事件数
3. 如果不对，纯文本替换 base64 字符串（注意 `"` 和 `;` 的位置）

---

## 11. 最终校验清单

预处理产包前逐个确认：

- [ ] 章节数 = 预期总数
- [ ] 每章 char_count 正确
- [ ] 所有章节通过保真校验
- [ ] 角色数 ≥ 主要角色 + 重要配角
- [ ] 每个角色有 identity 和 full_description
- [ ] 每个角色有 color
- [ ] key_events 覆盖 ≥ 80% 的章节
- [ ] key_events 总数 ≥ 角色数 × 2
- [ ] relations 总数 ≥ 角色数 × 3
- [ ] alias_mapping 覆盖所有别名变体
- [ ] graph.json 从 characters 派生（不手动写）
- [ ] manifest.json 的 stats 字段准确
- [ ] 独立版 HTML 嵌入数据验证通过

---

## 12. 版本与扩展

- 往后加字段一律**新增可选字段**，不改已有字段含义，`schema_version` 小版本号 +1
- 破坏性改动（如改标注语法）才升大版本，阅读器按 `schema_version` 拒绝不兼容的包
- "阅读数据市场"就是分享这个 `.bookpack`——格式稳定很重要

---

## 13. 数据量化参考目标

以下为面向中长篇小说（15-30 章，20-40 重要角色）的建议目标。按作品篇幅和角色数量等比缩放。

| 指标 | 建议取值 | 说明 |
|------|:--------:|------|
| 角色数 | 主要角色 + 重要配角 | 龙套不收录 |
| 事件数 | ≈ 角色数 × 5~10 | 每角色覆盖其出场章节的 60% 以上 |
| 关系数 | ≈ 角色数 × 5~8 | 太少图谱太稀疏，太多太密集 |
| 别名映射 | 覆盖所有称呼变体 | 含标点变体 |
| 内联标注 | 正文中角色名出现次数 | 通过保真校验确保准确 |
| 章节覆盖 | ≥ 90% | 关键章节必须有事件

---

## 附录：关键代码片段

### 两阶段标注核心逻辑

```python
def annotate_paragraphs(chars, paragraphs):
    all_patterns = [(alias, c.id, c.first_chapter) 
                    for c in chars 
                    for alias in c.aliases 
                    if len(alias) >= 2]
    alias_map = build_alias_mapping(chars)
    
    for paragraph in paragraphs:
        result = paragraph
        
        # 阶段一：收集所有匹配，最长优先
        matches = []
        for pattern, cid, fc in all_patterns:
            if pattern not in result:
                continue
            pos = 0
            while True:
                idx = result.find(pattern, pos)
                if idx == -1:
                    break
                matches.append((idx, idx + len(pattern), (pattern, cid)))
                pos = idx + len(pattern)
        
        matches.sort(key=lambda x: (x[0], -x[1]))
        selected = self_avoiding_walk(matches)
        result = apply_annotations(result, selected)
        
        # 阶段二：回退补漏
        fallback = find_unannotated_matches(result, alias_map)
        result = apply_annotations(result, fallback)
        
    return annotated
```

### 防重叠算法

```python
def self_avoiding_walk(matches):
    """贪心选最长的、不重叠的匹配"""
    matches.sort(key=lambda x: (x[0], -x[1]))
    selected = []
    for match in matches:
        if selected and match[0] < selected[-1][1]:
            continue
        selected.append(match)
    return selected
```

### 保真校验

```python
def fidelity_check(original_paragraphs, annotated_paragraphs):
    def strip(text):
        return re.sub(r'\[\[c:\w+\]\]|\[\[\/\]\]', '', text)
    for orig, ann in zip(original_paragraphs, annotated_paragraphs):
        if strip(ann) != orig:
            return False, "不匹配"
    return True, "通过"
```

### 检查是否在标注区域内

```python
def inside_annotation(text, pos, length):
    """检查位置是否在 [[c:...]]...[[/]] 内"""
    before = text[:pos]
    c_open = before.rfind("[[c:")
    if c_open == -1:
        return False
    c_close = before.rfind("[[/]]")
    if c_close > c_open:
        return False  # 最近的标注已关闭
    after = text[pos:]
    close_in_after = after.find("[[/]]")
    if close_in_after == -1:
        return True  # 标注未关闭
    if close_in_after < length:
        return False  # 跨过了标注边界
    return True
```

---

> **耗时参考**：以20章/26角色的小说为例，数据预处理 + 阅读器联调约 3 小时。核心时间花在角色数据的手工撰写（key_events + analysis + relations）。工具链熟练后可压缩到 1-2 小时。
>
> **批量生产建议**：
> - 章节切分和标注用脚本自动化
> - 角色数据用 LLM 辅助生成初稿再人工审查
> - relations 可基于 key_events 中共同出现的角色半自动生成
