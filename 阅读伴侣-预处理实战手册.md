# AI 阅读伴侣 · 书籍预处理实战手册

> 基于《百年孤独》预处理全流程提炼的通用方法。
> 适用任何虚构类书籍（小说/史诗/群像叙事）的 .bookpack 生成。

---

## 一、预处理总流程

```
原始文本 → 章节切分 → 角色提取 → 正文标注 → 数据丰富 → 打包校验
```

### 工具链
- Python 3（JSON 处理、文本分析、保真校验）
- 无需外部依赖，标准库即可（`json`, `re`, `zipfile`, `base64`）

---

## 二、章节归一化

### 原则
阅读器只需要干净的 `ch01.json ~ chNN.json`，永远不要暴露原格式的混乱。

### 操作
1. 从原始文本（epub/txt/md）按目录切分为 N 章
2. 每章存为 `{ "index": N, "id": "chN", "title": "第N章", "paragraphs": [...] }`
3. **清洗脏空格**：如"布恩迪亚 上校" → "布恩迪亚上校"

### 关键检查项
- [ ] 章节数与目录一致
- [ ] 每章有段落数组（而非大段文本）
- [ ] 无原始格式残留（如 `part0003.html#calibre_pb_x`）

---

## 三、角色库构建

### 3.1 角色识别

**必做清单**：
```
每个角色需要：
├── id           # 唯一标识（c01, c02...）
├── name         # 中文译名
├── original_name # 原文名（有译名差异时）
├── short_name   # 简短显示名（卡片/时间线用）
├── badge        # 同名区分角标（①, ②...）
├── aliases      # 所有可能的称呼变体
├── identity     # ⭐ 卡片默认头部（一句话简介，安全，不剧透）
├── full_description  # ⚠️ 完整描述（含剧透，需二次确认）
├── color        # 高亮色值
├── family       # 家族/阵营分组
├── first_chapter # ⭐ 首次登场章节（防剧透过滤关键字段）
├── key_events   # ⭐ 每章事迹数组
├── relations    # ⭐ 与其他角色的关系
├── analysis     # ✨ 文学分析/评论（非必填，但推荐）
└── portrait     # 头像文件路径
```

### 3.2 角色覆盖标准

| 角色类型 | 是否必须 | 说明 |
|---------|:-------:|------|
| 主角（书中反复出现） | ✅ | 完整字段 |
| 重要配角（推动剧情） | ✅ | 至少 identity + key_events |
| 次要配角（出现 1-2 章） | 可选 | 至少 identity + first_chapter |
| 龙套（仅提名字） | 不收录 | alias_mapping 也无需收录 |

### 3.3 命名规范

- `aliases` 中包含**所有**在该书翻译中出现的称呼变体
- 注意同名歧义：同一译名对应多人时，用 `badge` 区分
- `short_name` 要足够短以在卡片中显示

---

## 四、正文标注策略

### 4.1 两阶段标注法（核心方法）

```
阶段一：主要标注（用全部别名字符串匹配）
阶段二：回退标注（用 alias_mapping 补漏）
```

### 4.2 标注规则

**标记格式**：`[[c:角色ID]]原文中出现的名字[[/]]`

**匹配优先级**：
1. 最长匹配优先（"奥雷里亚诺·布恩迪亚上校" > "布恩迪亚上校" > "上校"）
2. 等长时选 first_chapter 最早的角色（早出现的角色更有可能）
3. 禁止嵌套/重叠标注

**最小长度**：建议 ≥2 字符，避免单字误标

### 4.3 歧义消歧

| 歧义类型 | 处理方案 |
|---------|---------|
| 同一名字指向多人（如"蕾梅黛丝"→c08/c24） | 选 first_chapter 最早的角色 |
| 短名包含在长名中（如"奥雷里亚诺"⊂"奥雷里亚诺第二"） | 最长匹配优先自动处理 |
| 同一人在不同章节身份不同 | $\textcolor{red}{\text{不做}}$ 上下文感知，标注最匹配的角色 |

### 4.4 保真校验（不可或缺）

```python
def fidelity_check(original_paragraphs, annotated_paragraphs):
    """剥离标注标记后，与原文逐字比较"""
    def strip(text):
        return re.sub(r'\[\[c:\w+\]\]|\[\[\/\]\]', '', text)
    for orig, ann in zip(original_paragraphs, annotated_paragraphs):
        if strip(ann) != orig:
            return False, "不匹配"
    return True, "通过"
```

**要求**：所有章节必须通过保真校验，否则 `manifest.integrity.text_verified = false`

---

## 五、角色数据丰富

### 5.1 key_events（时间线数据源）

时间线渲染完全依赖 `key_events` 数组。要求：
- **每章至少 3-5 条事件**，关键章节 10-20 条
- 每个事件格式：`{ "chapter": N, "event": "一句话描述" }`
- 覆盖该章所有活跃角色
- 事件描述简洁但信息完整（阅读器模板会用 `esc()` 输出为纯文本）

**量化目标**：总事件数 ≈ 角色数 × 每角色平均出场章节 × 1.5

### 5.2 relations（关系图谱数据源）

关系图谱渲染依赖 `relations` 数组。要求：
- 每个涉及到的关系双方都需要声明（A→B 和 B→A 最好都写）
- 关系格式：`{ "target_id": "cXX", "type": "血缘/爱情/友情/仇恨", "label": "具体关系", "based_on_chapters": [N] }`
- `based_on_chapters` 用于防剧透（只展示已读章节的关系）

**覆盖要求**：
- 直系血缘关系：必须覆盖
- 故事中明确建立的亲密/敌对关系：必须覆盖
- 同住/同期的社交关系：推荐覆盖
- 仅偶遇一次的关系：可省略

**量化目标**：总关系数 ≈ 角色数 × 3~5（太少了不够用，太多了图谱太密）

### 5.3 analysis（角色分析）

每段 150-250 字文学评论。内容覆盖：
- 角色在小���中的核心意义
- 其命运所代表的主题
- 与其他角色的核心冲突/联系
- 不回避结局（因为是剧透内容，需二次确认才展示）

---

## 六、alias_mapping 设计

### 用途
1. **回退着色**：阅读器正文高亮时，对内联标注未覆盖的短名进行正则匹配着色
2. **AI 问答消歧**：用户提问中提到名字时，映射到具体角色

### 构建规则
```python
def build_alias_mapping(chars):
    # 按 first_chapter 排序，让最早出现的角色获得冲突胜利
    entries = [(alias, cid, c.first_chapter) for c in chars for alias in c.aliases]
    entries.sort(key=lambda x: x[2])  # 最早的角色优先
    mapping = {}
    for alias, cid, _ in entries:
        if alias not in mapping:
            mapping[alias] = cid
    return mapping
```

### 注意事项
- 别名覆盖: 主标点格式（"·" vs "."）都要收录
- 不用删冲突——选最早的角色作为默认

---

## 七、graph.json 构建

可从 `characters.relations` 自动生成：

```python
nodes = [{ "id": c.id, "label": c.name, "color": c.color, ... } for c in chars]
edges = []
seen = set()
for c in chars:
    for r in c.relations:
        key = tuple(sorted([c.id, r.target_id]))
        if key not in seen:
            seen.add(key)
            edges.append({ "source": c.id, "target": r.target_id, ... })
```

---

## 八、常见坑与排查

### ❌ 时间线空白
**症状**：数据加载了（271 events）但时间线无内容
**排查**：
1. 检查 `chars.forEach` 中是否有静默异常 → 加 `try-catch`
2. 检查 `c.key_events` 是否为数组 → `if(!Array.isArray(c.key_events)) return`
3. 检查 `e.chapter` 是否为数字 → `if(chNum===undefined||chNum===null) return`
4. 检查阅读器 JS 是否有嵌套模板字面量语法错误 → 改用字符串拼接

### ❌ 关系图节点/边不全
**排查**：
1. `relations` 是否在 `characters.json` 中正确声明
2. `graph.json` 是否从最新的 `characters.json` 重新生成
3. 边是否去重（双向关系只存一条）

### ❌ 保真校验失败
**排查**：
1. 标注标记是否嵌套 → 用 `self_avoiding_walk` 防止重叠
2. 标注使用的名字是否与原文完全一致（含标点）
3. 清洗阶段是否有不一致的替换

### ❌ 嵌入 base64 没更新
**排查**：
1. 用 `python` 解码检查独立版 HTML 中的 `EMBEDDED_PACK`
2. 解压后验证 `characters.json` 的事件数
3. 如果不对，纯文本替换 base64 字符串（注意 `"` 和 `;` 的位置）

---

## 九、最终校验清单

预处理产包前逐个确认：

- [ ] 章节数 = 预期总数
- [ ] 每章 char_count 正确
- [ ] 所有章节通过保真校验
- [ ] 角色数 ≥ 主要角色 + 重要配角
- [ ] 每个角色有 identity 和 full_description
- [ ] 每个角色有 color
- [ ] key_events 覆盖 ≥ 80% 的章节
- [ ] key_events 总数 ≥ 角色数 × 2
- [ ] relations 总数 ≥ 角色数 × 3
- [ ] alias_mapping 覆盖所有别名变体
- [ ] graph.json 从 characters 派生（nodes 和 edges 不手动写）
- [ ] manifest.json 的 stats 字段准确
- [ ] 独立版 HTML 嵌入数据验证通过

---

## 十、以百年孤独为参考的数据目标

| 指标 | 参考值 |
|------|:------:|
| 角色数 | 26 |
| 事件数 | 271 |
| 关系数 | 203 |
| 别名映射 | 52 |
| 图谱边数 | 127 |
| 内联标注 | ~2,800 |
| 章节覆盖 | 20/20 |

**经验公式**：
```
事件数 ≈ 角色数 × 10
关系数 ≈ 角色数 × 8
```

适用于中长篇小说（15-30 章，20-40 重要角色）。

---

## 附录：关键代码片段

### 两阶段标注（Python 核心逻辑）

```python
def annotate_paragraphs(chars, paragraphs):
    all_patterns = [(alias, c.id, c.first_chapter) for c in chars for alias in c.aliases if len(alias) >= 2]
    alias_map = build_alias_mapping(chars)
    
    for paragraph in paragraphs:
        # 阶段一：主要标注
        matches = find_matches(paragraph, all_patterns)
        selected = self_avoiding_walk(matches)
        result = apply_annotations(paragraph, selected)
        
        # 阶段二：回退标注
        fallback = find_unannotated_matches(result, alias_map)
        result = apply_annotations(result, fallback)
```

### 防重叠算法

```python
def self_avoiding_walk(matches):
    """贪心选最长的、不重叠的匹配"""
    matches.sort(key=lambda x: (x[0], -x[1]))  # start asc, length desc
    selected = []
    for match in matches:
        if selected and match[0] < selected[-1][1]:
            continue
        selected.append(match)
    return selected
```

---

> **最后提醒**：本书的数据预处理 + 阅读器联调花了约 3 小时（含反复修复 bug 的时间）。核心时间花在角色数据的手工撰写（key_events + analysis + relations）。如果是批量处理多本书，建议：
> - 先自动化章节切分和标注（脚本跑）
> - 角色数据用 LLM 辅助生成初稿再人工审查
> - relations 可以半自动生成（基于 key_events 中共同出现的角色）
